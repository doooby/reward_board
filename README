# HOW TO
see demo

config:
element: HTMLElement;
position?: { x: number, y: number };
onStepRequested?: (position: { x: number, y: number }) => void;

# DEVELOPMENT
`` yarn install
`` yarn webpack --watch
`yarn demo:serve`# this runs on default: `http://localhost:3000`
